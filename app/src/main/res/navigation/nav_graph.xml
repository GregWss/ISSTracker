<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/mainActivity">

    <activity
        android:id="@+id/mainActivity"
        android:name="uqac.ca.isstracker.Activities.MainActivity"
        android:label="activity_main"
        tools:layout="@layout/activity_main" />
    <activity
        android:id="@+id/dashboardActivity"
        android:name="uqac.ca.isstracker.Activities.DashboardActivity"
        android:label="activity_dashboard"
        tools:layout="@layout/activity_dashboard" />
    <activity
        android:id="@+id/mapActivity"
        android:name="uqac.ca.isstracker.Activities.MapActivity"
        android:label="activity_map"
        tools:layout="@layout/activity_map" />
    <activity
        android:id="@+id/humansActivity"
        android:name="uqac.ca.isstracker.Activities.HumansActivity"
        android:label="activity_humans"
        tools:layout="@layout/activity_humans" />
</navigation>